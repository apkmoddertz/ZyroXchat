rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global read and write access for all documents to guarantee zero permission issues
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
